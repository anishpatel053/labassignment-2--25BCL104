#include <stdio.h>

int main() {
    int n, i;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    printf("first %d even numbers are:\n", n);

    for (i = 0; i <= n-1; i++)
    {
        printf("%d\n", 2*i);
    }

    return 0;
}



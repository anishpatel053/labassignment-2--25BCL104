#include <stdio.h>
int main() {
int num, digit;

printf("enter a number: ");
scanf("%d", &num);

printf(" seprated digits are:\n");

    while (num > 0)
     {
        digit = num % 10;
        printf("%d\n", digit);
        num = num / 10;
     }

return 0;
}
